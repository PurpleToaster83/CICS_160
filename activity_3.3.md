Acitity 3.3 Password Checker Plan

have the user enter a password

bool is true

if statement with clauses for len greater than 8, 1st and 2nd character are not the same and in capital letter set, 
    if so then bool should become false

set sum to zero
check over all characters in string and if in numeric set then add to sum
    if not set bool to false
while checking over all characters check if in numeric or letters set

if sum is not even
    set to false

print bool good or bad